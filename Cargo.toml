[package]
name = "rustop"
version = "0.2.0"
edition = "2024"

# NOTE: This project uses just for task running.
# Install it globally: cargo install just

[dependencies]
ratatui = "0.30"
sysinfo = "0.39"
crossterm = "0.29"
signal-hook = "0.3"

[profile.release]
lto = true
codegen-units = 1
