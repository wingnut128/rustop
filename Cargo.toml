[package]
name = "rustop"
version = "0.1.0"
edition = "2024"

# NOTE: This project uses just for task running.
# Install it globally: cargo install just

[dependencies]
ratatui = "0.30"
sysinfo = "0.39"
crossterm = "0.29"

[profile.release]
lto = true
codegen-units = 1
