[package]
name = "rustop"
version = "0.1.0"
edition = "2024"

# NOTE: This project uses cargo-make for task running.
# Install it globally: cargo install cargo-make

[dependencies]
ratatui = "0.30"
sysinfo = "0.38"
crossterm = "0.29"

[profile.release]
lto = true
codegen-units = 1
